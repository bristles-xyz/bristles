import { describe } from 'vitest'
// import { initViewportState, updateWheelPan, getCoordinatesFromClientScreen } from './viewport'

describe('Viewport functions', () => {
  /*
  test('updateWheelPan should update the pan state correctly', () => {
    const delta = { x: 5, y: 10 } // Example delta values
    const newState = updateWheelPan(initViewportState, delta)

    // Check if the new state has been updated correctly based on the delta values
    expect(newState.offset.x).toBe(initViewportState.offset.x - delta.x)
    expect(newState.offset.y).toBe(initViewportState.offset.y - delta.y)
  })

  test('getCoordinatesFromClientScreen should return correct coordinates', () => {
    const delta = { x: 5, y: 10 } // Example delta values
    const newState = updateWheelPan(initViewportState, delta)

    const point = { x: 20, y: 30 } // Example point values
    const coordinates = getCoordinatesFromClientScreen(newState, point)

    // Check if the coordinates are calculated correctly based on the initial state and point
    expect(coordinates.x).toBe(point.x - newState.offset.x)
    expect(coordinates.y).toBe(point.y - newState.offset.y)
  })
  */
})
